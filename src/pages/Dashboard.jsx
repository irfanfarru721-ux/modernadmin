import React, {useEffect,useState} from 'react'; import API from '../services/api'; export default function Dashboard(){ const [s,setS]=useState({vendors:0,products:0,categories:0,users:0}); useEffect(()=>{ Promise.all([API.get('/vendors'),API.get('/products'),API.get('/categories'),API.get('/user')]).then(([vs,ps,cs,us])=>setS({vendors:vs.data.length,products:ps.data.length,categories:cs.data.length,users:us.data.length})).catch(()=>{}); },[]); return (<div><h1>Dashboard</h1><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(160px,1fr))',gap:12}}><div className='card'><h3>{s.vendors}</h3><p>Vendors</p></div><div className='card'><h3>{s.products}</h3><p>Products</p></div><div className='card'><h3>{s.categories}</h3><p>Categories</p></div><div className='card'><h3>{s.users}</h3><p>Users</p></div></div></div>); }
